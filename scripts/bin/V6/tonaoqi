#!/bin/sh

if [ $(whoami) != root ]; then
  sudo $0 $*
  exit 0
fi

cp ../../files/V6/autoload.org /opt/aldebaran/etc/naoqi/autoload.ini
cp ../../pref/ALMotion.xml /opt/aldebaran/etc/naoqi/ALMotion.xml
